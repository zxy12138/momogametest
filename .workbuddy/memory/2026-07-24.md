# 2026-07-24 · 工作日志

## 角色放大后的视觉比例 + 待机速率修正
- 用户反馈：v3.0 角色放大到 130×250 后，房间门/下一层传送门/驿站等可见色块仍过小（≈56×70），且替换待机素材后 idle 动画发飘。
- 诊断：
  - 门/传送门/驿站是 `RoomManager._build_doors`、`Game._spawn_next_door`、`RoomManager._build_inn` 的纯色 ColorRect+Label，尺寸写死；地板是纯色 ColorRect 无瓦片；房间 W=880/H=500 已能容纳大角色。
  - idle 速率快是 `Player.gd` spec `fps=12` 偏高；实测 `A-001_miai_idle.png`=520×250（4 帧×130 宽）切片正确，非尺寸错。
- 修复（用户确认：idle→6fps 等比 walk/run；门等比到角色尺寸）：
  - `Player.gd`：idle 12→6、walk 12→8、run 16→10。
  - `RoomManager.gd`：门 frame 56×70→130×160、portal 50×64→120×150、标签 14→22；驿站标记 60×60→150×150、标签 14→22。碰撞不动。
  - `Game.gd`：下一层传送门 frame 58×74→130×160、portal 54×70→120×150、标签 14→22。碰撞不动。
- 记录：docs/CHANGELOG.md 追加「2026-07-24 角色放大后的视觉比例与待机速率修正」条。

## 贴图场景构建 + 角色 0.6 缩放（v3.1）
- 用户确认：地图场景此前**完全没用**生成的 T-* 贴图（地板纯色 ColorRect、墙隐形碰撞体、门青色 ColorRect）；要求按 GDD（§4/§7/§9）建贴图场景，且「改无法进入的无法进入」，角色在 880×500 房里屏占比过大要缩小。
- 决策（用户拍板）：角色缩 0.6×；完整贴图场景（地板瓦片化 + 按层贴墙 + 门口门框 + 驿站贴图）。
- 实现（工程负责人 Agent 调度器报错，主理人直接落地）：
  - `Room.tscn`：Floor 节点 `ColorRect`→`TextureRect`。
  - `RoomManager._build_floor`：平铺 `T-000_base_dream_floor_tile_1.png`（STRETCH_TILE）+ 按层 modulate 染色。
  - `RoomManager._build_wall_visuals`（新增）：四边平铺对应层墙瓦片（层1 `T-021_office`/层2 `T-031_subway`/层3 `T-041_warped`/兜底 `T-001_base`），**仅 `neighbors` 边留 64px 门洞，其余实墙不可穿**；碰撞 StaticBody2D 不变。`_wall_seg` 辅助生成 TextureRect。
  - `RoomManager._build_doors` / `Game._spawn_next_door`：青色 frame/portal → `Sprite2D` 门框（`T-003_door_frame_open_anim.png`，4 帧横排表取首帧，缩放到 64×96）；触发器 44×44 不动。
  - `RoomManager._build_inn`：加 `T-050_dream_rest_stop_interior.png` 地面贴图（z=-3000）+ 暖光。
  - `Player._ready`：`_sprite.scale = Vector2(0.6,0.6)`（仅视觉，CharacterBody2D 碰撞不动）。
  - z_index：地板 -4000 / 墙 -10 / 驿站贴图 -3000 / 门框 4 / 标签 6。
- 验证：Godot 4.7.1 `--headless --script` 解析校验；修掉 1 个严格模式错误（`iscale` 用 `max()` 推断失败 → 改 `var iscale: float = ...`）；其余 `GameManager/MapData` 报错是 `--script` 环境无 Autoload 的误报（非本次引入）。7 个新贴图引用全部在磁盘（0 缺失），旧青色门块无残留。
- 注：Agent 工具（engineering-lead）连续两次报 `Cannot read properties of undefined (reading 'history')` 内部错误，派单失败，故主理人直接实现并保持 oversight。

## 场景构建方案切换为预制整图（v4.0 · 文档已更新）
- 用户决定放弃"通用瓦片拼接"流程：改为**每一关场景由美术预制整图**（一图一房，960×540，含墙/地板/家具/灯光全部烘焙），工程只需实现「门的开关动画」与「空气墙 InvisibleWall」的逻辑。用户参考图：`assets/tiles/changjing1.png`（第一层办公室类型，工位林立+文件散落）。
- 修改文档（**仅文档，代码未改**）：
  - `梦境逐影_游戏设计文档_最终版.docx`（已直接覆盖）：
    - §9.1 场景结构表：RoomManager.gd 行重写（加载预制整图 + 叠加空气墙 + 放置门节点 + 怪物刷新）；新增 Door.tscn/Door.gd 与 InvisibleWall.tscn 两行（表行数 13 → 15）。
    - §4.3 末尾新增「场景视觉呈现」说明段（含 changjing1.png 引用）。
    - §7.2/§7.3/§7.4 各新增「【场景构建方式】」说明段，明确本层用预制整图。
  - `梦境逐影_美术素材清单_最终版.xlsx`：**原文件被 wps.exe (PID 44676) 长期持有排他锁，无法直接覆盖**。v4.0 内容已写到 sibling 文件 `_梦境逐影_美术素材清单_最终版_v4.0.xlsx`，用户需关闭 WPS 后手动改名覆盖。
    - Sheet3 重构为 5 段：场景整图 S-001~S-018（3 层 × 6 房型 960×540）/ 门动画 D-001~D-003 / 驿站内饰 I-001~I-003 / 通用道具 P-001~P-003 / 地图节点 N-001~N-010 + F 段废弃清单（T-000/T-001/T-020~T-044 共 21 项标 ✗）。
    - Sheet6 总览更新为 v4.0：Tileset 行移除，新增「场景整图·第一层/二/三层（6 房）」与「门动画」行。
- 实现踩坑：
  - Python `xml.etree.ElementTree` 没 `addnext`/`getparent` → 改用 `parent.insert(idx+1, new_p)` 插入新段。
  - `ET.tostring(root, ..., standalone=True)` 在 Python 3.13 不可用 → 手工拼 `<?xml ... standalone="yes"?>\n` 头。
  - `<row>` 是 `<sheetData>` 的子元素，不是 worksheet 直接子元素；首版脚本遍历 `list(root)` 找不到 row，旧数据残留 + 新数据错位。修复：先 `root.find(NS+'sheetData')` 再清/加 row。
  - WPS (wps.exe) 长期持有 xlsx 排他锁，普通 `os.replace` / `[System.IO.File]::Copy(src, dst, true)` 都 PermissionError；只能写 sibling 文件让用户手动覆盖。
  - PowerShell 删除中文名 lock 文件需用 Get-ChildItem 拿 FileInfo 对象后 `Remove-Item $_.FullName -Force`（直接传中文路径会变成 `~.xlsx` 找不到）。
- ⚠️ **工程代码仍是 v3.1 通用瓦片版**（RoomManager 的 `_build_wall_visuals` 等还在），与新文档规范不一致；待用户美术整图就绪后，工程侧按 v4.0 重写 RoomManager（删瓦片拼墙 → 改 `load(scene_png)` + InvisibleWall + Door 节点挂载）。
- 记录：docs/CHANGELOG.md 追加「2026-07-24（续2）· 场景构建方案切换为预制整图（v4.0）」条；MEMORY.md 美术管线段更新为 v4.0。
- 工具脚本：`tools/_doc_modify_gdd.py`（改 GDD docx）、`tools/_doc_modify_xlsx.py`（写 v4.0 xlsx 到 sibling）。

## 角色再缩小 + changjing1 预制整图试用（v4.0 代码第一步）
- 用户反馈：人物还是太大，"起码缩小一半以上"；并希望"用我提供的地图 changjing1.png 作为一个战斗场景试试"。
- 实现：
  - `Player.gd`：`_sprite.scale` 0.6 → **0.28**（约 53% 缩减，满足"缩小一半以上"）；仅视觉，碰撞不动。
  - `Enemy.gd`：`setup` 新增 `_sprite.scale = Vector2(0.45, 0.45)`。原因：敌人原本就是原生 1.0（比 0.6 的玩家还大），若只缩玩家会让战斗场景里"玩家很小、敌人巨大"不协调；取 0.45 保持原 0.6:1.0≈0.28:0.45 比例。碰撞半径 `mini(fw,fh)*0.32` 不变。
  - `RoomManager.gd`：新增 `_prefab` 标记；`_build_floor` 读 `_data.scene_img`——有则 `load` 该图作背景（`STRETCH_KEEP_ASPECT_COVERED`，`z_index=-4000`）并跳过瓦片墙体贴图（`_build_walls` 中 `if not _prefab` 才 `_build_wall_visuals`）；碰撞墙/门逻辑不变。
  - `LevelData.gd`：第一层 `r2`/`r3` 临时加 `scene_img="res://assets/tiles/changjing1.png"`（标注 TEST，待正式整图流程接管后删除此键）。
  - `Game.gd`：dev 标签 "F2 切换" → "F12 切换"（与 project.godot 实际绑定 keycode 4194315=F12 一致，旧标签误导）。
- 验证：Godot 4.7.1 `--headless` 临时把 main_scene 指向 Game.tscn + `_chk` autoload 进入 r2（预制整图背景 + 刷 rescale 敌人），日志 `CHK_START`→`RT_OK_ENTERED_R2`，EXIT=0，零 `SCRIPT ERROR`/`Invalid access`。验证后还原 project.godot 与删除 `_chk.gd`。
- ⚠️ 注意：本次只把 v4.0 的"加载预制整图背景"第一步跑通，门动画（Door 开/关帧）与空气墙（InvisibleWall）逻辑尚未实现；当前仍用 RoomManager 旧碰撞墙 + T-003 门框贴图。待用户整图资产（S-001~S-018）就绪后，按 v4.0 补齐 Door/InvisibleWall 并重写场景加载。
- 记录：docs/CHANGELOG.md 追加「2026-07-24（续3）· 角色再缩小 + changjing1 预制整图试用」条；MEMORY.md v4.0 段更新。

