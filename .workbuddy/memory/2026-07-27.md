# 2026-07-27 工作日志

- **新游戏开头动画**：新增 `src/scenes/Intro.tscn`(Control 根：VideoStreamPlayer 全屏 + 右上角 SkipHint Label) 和 `src/scenes/Intro.gd`(播放 / ESC 跳过 / finished 自动进下一场景)。`Main._new_game()` 改指向 `Intro.tscn`，intro 播完/跳过后进 `WeaponSelect.tscn`；「继续」路径不变，暂停「重新开始」也不重播 intro（避免每次死亡重看）。
- **mp4→ogv 转码**：Godot 4 桌面端 `VideoStreamPlayer` 默认不能可靠解 H.264/mp4（无内置解码器），原生稳定支持 Theora `.ogv`。用 `imageio-ffmpeg` 静态 ffmpeg 把 `assets/Anime/A_001.mp4` 转成 `A_001.ogv`(libtheora -q:v5 + libvorbis -q:a4, ~15s, 26MB)，原 mp4 保留。`Intro.tscn` 引用 ogv；如需换回 mp4 改 ext_resource 路径即可。
- 验证：本机无 Godot，做代码级核查（类型显式、ui_cancel 与 Game.gd 暂停 ESC 一致、路径衔接正确、stream 为空时 ESC 仍可用防卡死）。
- 注：`imageio-ffmpeg` 装在 managed python 的 site-packages 里（`C:\Users\17930_ueiiii0\.workbuddy\binaries\python\versions\3.13.12\Lib\site-packages\imageio_ffmpeg`），ffmpeg 静态二进制位于其 `binaries/` 下，可复用。

- **开头动画"显示不全"修复（扩展名坑）**：用户重生成的 1080p 文件叫 `A_001.Ogg`（大写 O），放进 Godot 被当成 OggVorbis **音频**导入，`VideoStreamPlayer.stream` 拿不到 VideoStream → 视频不显示。Theora 视频**必须 `.ogv` 扩展名**才被 `VideoStreamTheoraImporter` 当视频导入。修法：把 `A_001.Ogg`(theora 1920×1080) 改名为 `A_001.ogv` 覆盖旧的 1440p；旧 1440p 备份为 `A_001_1440p_old.ogv`（含 `.uid`/`.import`）。`Intro.tscn` 本就引用 `A_001.ogv` 无需改路径。Godot 4.7.1 无头加载验证 `CHK_INTRO_OK stream=res://assets/Anime/A_001.ogv`，EXIT=0。详见 CHANGELOG 2026-07-27（补2）。

- **开头动画铺满全屏 + 提示置顶（补3）**：用户反馈视频只显示左上角小块、未全屏。根因：`Intro` 根节点是默认 `Control`，不自动铺满视口，子 `VideoStreamPlayer` 用「填满父」锚点只填满那小块。修法：`Intro.tscn` 根节点设 `layout_mode=3`(FULL_RECT) 强制铺满；`VideoPlayer` `z_index=0`，`SkipHint` `z_index=1` 置顶在视频前。无头验证 `global_pos=(0,0) stream_ok=true`、`hint_z=1>vp_z=0`。详见 CHANGELOG 2026-07-27（补3）。

- **修复 ESC 跳过开场视频失效（补4）**：补3 用 `Write` 整体重写 `Intro.tscn` 时，**漏掉根节点 `script = ExtResource("1")`**，导致 `Intro.gd` 整脚本静默未挂载——画面/提示照常（autoplay 和文字写死在 tscn 里），但 `_ready`/`_input`/`_skip` 全不执行，ESC 失效、视频播完也不自动进选武器。诊断：无头注入 ESC 后 `current_scene.has_method("_skip")==false` 确认脚本没挂。修法：根节点补 `script = ExtResource("1")`；并把 `_unhandled_input` 改 `_input`（根节点是 Control，`ui_cancel` 会在 GUI 阶段被 Control 消费，`_input` 最前置更稳）。无头验证：`CHK_HAS_SKIP=true`、直调 `_skip` 与注入 ESC 均切到 `WeaponSelect`(`CHK_DIRECT_OK`/`CHK_ESC_OK`)。**教训：整体 `Write` 重写 `.tscn` 必须保留根节点 `script=` 行，否则脚本静默失效且画面照常，极难察觉。** 详见 CHANGELOG 2026-07-27（补4）。

- **新建项目技能 `godot-8dir-char-anim`（8 向角色动画生成）**：官方市场无 "character animation creator" skill（搜 character animation/sprite/godot/8 direction 均无匹配），应客户要求量身做了一个项目级技能并装好。位置 `.workbuddy/skills/godot-8dir-char-anim/`（SKILL.md + references/convention.md + scripts/pack_8dir.py）。内容：把现有 Player 的 3 向+镜像升级为真正 8 向（down/down_right/right/up_right/up/up_left/left/down_left，DIR_ORDER 从「下」顺时针）；命名 `A-001_miai_{action}.png`（每动作一张 8 行×F 列@130×250 图集）；`make_frames` spec 格式 `[路径,帧宽,帧高,帧数,fps,行号]`；含角度→方向解析器 `dir_from_vector(v)` 与 `_anim_update` 真 8 向模板（及 4 向+镜像省美术版）；ImageGen 提示词模板（角色描述逐张复用保一致，对角线由右/上旋转 45° 派生）；`pack_8dir.py` 打包 {action}/{dir}.png 水平帧条成 8 行图集。已通过 skill-creator 校验打包。客户选了「全套：美术+Godot接线」。

- **开头动画花屏修复（补5）**：现象=功能正常但画面花屏。根因（ffprobe/ffmpeg 实测，非推断）：用户"重新生成的1080p文件"A_001.Ogg→A_001.ogv 的 **theora 流本身损坏**（解码前120帧564行 `unpack_block_qpis` 错误，905帧中730帧dup重复）；Godot 解损坏流→花屏，功能正常仅因解码器容错未崩。对照：旧 A_001_1440p_old.ogv 解码0错误（干净）；`A_001.mp4`（H264/2560×1440/yuv420p/24fps/15s/AAC）解码0错误=干净源。修复：从干净 mp4 重编码 theora（1920×1072，高度16对齐裁8px，yuv420p，全关键帧，-r 30，-an），覆盖 A_001.ogv(61MB)，损坏源备份 A_001_1080p_src_before_align.ogv；ffmpeg 复解新 ogv 0 错误。详见 CHANGELOG（补5）。**教训：AI/工具导出的 theora(.ogv) 务必 ffprobe 验解码错误数（不只看 codec 名）；theora 帧高须16对齐（1080非对齐，1072/1088/1440对齐）。**

- **武器场景内拾取 / 开场序列**：见 CHANGELOG_2026-07-27_weapon_in_scene.md。核心：新游戏 `Main._new_game` 置 `prologue_pending`，`Intro` 播放→`Game.tscn`→`_ready` 见 flag `reset_run("")` + 播「醒来」序列（zoom 2→3.4 / 头顶对话框 / 3.4s 或 ESC 跳）→ `_end_prologue` 生成 3 把初始武器。地面武器 `WeaponPickup` (Node2D) + F（interact=physical 70）拾取/交换（旧武器掉地，0.6s 免疫防死循环）。HUD「未装备」+ 提示框挂 CanvasLayer。**改完顺手修了两个老 bug**：`get_weapon()` 无武器返回 `{}`（而非 nil，所有调用方要 `is_empty()` 才安全），新建 `class_name` 在无头解析期找不到（改 preload + Node2D + call/get/set 破循环）。

- **三 BUG 修复 v2（2026-07-27 续）**：①相机 anchor_mode=1 把角色钉死左上；场景改成 0 (DRAG_CENTER) 且 `_play_prologue/_end_prologue` 删掉 anchor_mode 切换。②重启后地面武器消失（仅 `_end_prologue` spawn，`prologue_pending` 已 false），`_swap` 末尾 `if rid=="r1" and not prologue_pending` 也 spawn；`_spawn_starter_weapons` 跳过 `weapon_id` 避免重复。③三把武器素材重画：`tools/gen_weapon_icons.py` 用 managed venv+PIL 4× 超采样生成 96×96 PNG，覆盖 W-001/002/003；`WeaponPickup` 缩到 0.8 + `Weapon.gd` 手持缩 0.5 让武器明显。无头测试 prologue 生成 3 把 / 重开模拟后 3 把，PASS。详见 CHANGELOG_2026-07-27_weapon_in_scene_bugfixes.md。

- **新游戏武器选择改为「场景内拾取」（需求：开头后进第一场景，3 把武器散落地上，F 交互，可换）**：
  - 流程变更：`Main._new_game` 置 `GameManager.prologue_pending=true` → `Intro`(NEXT_SCENE 改为 `Game.tscn`，不再经 `WeaponSelect`) → `Game._ready` 见 prologue_pending 则 `reset_run("")`(开局无武器) 并播**开场序列**。
  - 开场序列(`_play_prologue`)：锁输入 → 玩家相机 `anchor_mode` 临时改 0(居中)+`zoom` 2→3.4(拉近) → 头顶 Control 对话框"我醒来了，这是在哪……" → 3.4s 后或**按 ESC 跳过**(`_input` 消费 ui_cancel 并 `_end_prologue`)：zoom 回 2、`anchor_mode` 还原 1、清对话框、解锁、生成 3 把起始武器。
  - 新节点 `src/weapons/WeaponPickup.gd`(class_name WeaponPickup, extends Node2D)：导出 `weapon_id`，运行时用图标建 Sprite2D+浮动动画；`just_dropped()`(0.6s 免疫，防交换瞬间又换回)、`can_interact()`、`prompt_text()`。
  - 拾取/交换(`Game._pick_up_weapon`)：按 F 拾起；若已持武器则旧武器掉脚下(免疫 0.6s)；`GameManager.weapon_id` 切换即生效(Weapon.gd 自动重建手持贴图)。`_pickups` 数组 `_swap` 清。
  - 邻近检测在 `Game._physics_process`：玩家 64px 内最近的 `can_interact` 武器 → 屏幕底部 `[F] 拾取 XX` 提示框(`_pickup_panel`，挂 `_ui_layer` 屏幕空间)。
  - HUD：无武器显示"武器：未装备"；操作提示加"· F 拾取/交互"。
  - `F`=`interact` 动作早已绑定 physical_keycode 70，直接复用。
  - **headless 校验通过**：`_chk.gd` 临时 autoload(`--run-prologue-test`)注入 ESC/F，验证 序列激活→ESC 跳过→生成3把→拾取 staff→交换 sword→走回换回 scythe 全 OK，EXIT=0；已删 `_chk.gd` 并还原 main_scene/autoload。
  - **两个连带坑已修**：① 全局 `WeaponPickup` class_name 在无头解析期找不到（headless 不复扫 global_script_class_cache）→ `Game.gd` 改用 `preload` 拿脚本、`Array[Node2D]`+`call/get/set` 访问成员，类型不依赖全局类名（与项目既有 headless 教训一致）。② `GameManager.get_weapon() -> Dictionary` 在 `weapon_id=""` 时返回 null 触发运行时报错 → 改为空 `{}` 返回，且 `Weapon.gd`/`HUD.gd` 用 `w.is_empty()` 判无武器。
  - 注意：`WeaponSelect.tscn/.gd` 现已无人引用（死代码），保留未删；如要清理可移除。
