# 2026-07-28 工作日志

- 补15（相机回退 fc08289 + input_locked 真因）：用户复测删 .godot/重启后仍刷 `Game.gd:698 input_locked on Node (GameManager.gd)`，相机仍左上角。穷尽核查：6 个 autoload/依赖脚本(GameManager/SaveManager/MapData/_restart_test/Weapons/FloatingText)全干净、无循环 class_name；`.godot`(uid_cache/global_script_class_cache/filesystem_cache) 与 `.uid` 均正常 → 磁盘与缓存无损坏。结论=运行期 Godot 进程手里的 GameManager.gd 是陈旧实例(内存级)，磁盘干净但进程内存陈旧。相机左上角是下游症状：Game._ready 第49行 `GameManager.input_locked=false` 崩溃→初始化链断→相机停默认位。已 `git checkout fc08289 -- src/player/Player.tscn src/player/Player.gd` 回退相机节点(anchor_mode=0/enabled默认true/无 current/make_current)。根治=关闭 Godot 后删 .godot(运行中删无效)、且 H: 若是云同步盘须先暂停同步否则 .godot 被同步回陈旧版。CHANGELOG 补15；MEMORY.md 第20行(input_locked)与第33行(相机)已重写为准确结论。
- 关键教训：本项目上帝脚本(.gd)经外部 Python 改写后，若编辑器开着，进程内脚本实例会陈旧且删 .godot 被重建；必须关编辑器再删。云同步盘会复活 .godot。
- 补16（相机显式激活 + input_locked 全守卫）：用户要求"先把视角问题解决掉"并贴 补6 日志。重做分析——4 个 autoload + Weapons/FloatingText 全干净、无 BOM/不可见字符、无 class_name 冲突、无 GameManager 遮蔽；故 input_locked 报错=运行期缓存/陈旧实例(非源码)。改动：① Player.tscn 相机加 `current = true` + `enabled = true`(声明式激活，规避 GDScript `cam.current=true` 报 Invalid assignment)；② Player.gd _ready 顶部(任何 GameManager 调用前) `cam.make_current()` 解耦相机与 GameManager；③ 全部 `GameManager.input_locked` 读写改用 `_set_gm_locked/_is_gm_locked` 守卫(`"input_locked" in GameManager` 先判存在，避免每帧刷屏与 _ready 中断)；Player._ready 的 `make_frames` 也加守卫。注意：全局替换 `GameManager.input_locked`→`_is_gm_locked()` 曾误伤守卫函数体(变成 `_is_gm_locked() = v`/递归 `return _is_gm_locked()`)，已修回 `GameManager.input_locked`。Enemy/Boss 内 make_frames 未守卫(仅 GameManager 正常后触发)。根因仍是 .godot 缓存/云同步，须关编辑器删 .godot 根治；守卫让相机立即居中、游戏可跑以便验证。
- 补17（相机改确定性手动驱动，去掉放大仅居中）**：用户复测补16 仍左上角+input_locked 刷屏，明确要求"恢复、去掉视角放大、居中、保留武器"。决断：**彻底放弃 Camera2D.current/make_current 路线**——本 Godot 4.7.1 实测 make_current 不接管视口、enabled 单相机不自动激活，且补16 对 Player.tscn 的 current/enabled 编辑因 old_string 不匹配静默未生效(文件仍是 fc08289 旧值)。改法：`Player.tscn` Camera2D 设 `enabled=false`；`Player.gd _ready` 删 make_current 块；`Game.gd` 新增 `_update_camera()` 每帧写 `get_viewport().canvas_transform = Transform2D(_cam_zoom, center - _cam_zoom*player.global_position)`(center=视口size/2)→数学保证玩家居中，与 stretch_mode=canvas_items 兼容；`_physics_process` 顶部(早于开场/锁输入提前 return)调用，开场/锁输入期间也居中。删 `_play_prologue` 的 cam.zoom→3.4 拉近、`_end_prologue` 的 cam.zoom→2 复位；`_cam_zoom` 默认 1.0。保留：开场对话框(_ui_layer) + `_end_prologue` 的 `_spawn_starter_weapons()`(武器全保留)。此方案与 GameManager 是否加载无关——即便 GameManager 陈旧+input_locked 守卫兜底，玩家也稳定居中可移动。CHANGELOG 补17；MEMORY.md 相机条目(第33行)与开场序列条目(第38行)重写。
- 补18（手动相机整屏倾斜修复）：用户截图显示房间整体旋转。根因是 `Game.gd::_update_camera()` 把 `_cam_zoom=1.0` 传给 `Transform2D(z, position)`；Godot 4 双参数构造第一个参数是弧度旋转，不是缩放，故产生约 57.3° 倾斜。改为显式三向量无旋转缩放矩阵（X=`Vector2(z,0)`、Y=`Vector2(0,z)`、origin=`center-z*player_pos`），保留居中跟随与武器逻辑。Godot 4.7.1 无头加载 Game.tscn/编辑器解析均 EXIT=0，无脚本错误；记录见 `docs/CHANGELOG.md` 补18 与 `docs/CHANGELOG_2026-07-28_camera_rotation_fix.md`。
- 补19（地图背景图）：用户提供 `assets/ui/map/Maps_001.png`（羊皮卷轴素材），要作 M 键地图 UI 背景。`src/ui/MapUI.gd::_build_panel()` 在 `_panel` 已建后、`dim` 之前插一个 `TextureRect`(`load("res://assets/ui/map/Maps_001.png") as Texture2D` + `expand_mode=IGNORE_SIZE` + `stretch_mode=KEEP_ASPECT_COVERED` + `size=_panel.size` + `MOUSE_FILTER_IGNORE`)，是 _panel 第一个子节点确保渲染在最底；`dim.color.a` 0.82→0.30 让羊皮纸纹理透出。无头校验无脚本错。后续：替换图直接覆盖同名 PNG。CHANGELOG 补19。
