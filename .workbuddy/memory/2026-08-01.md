# 2026-08-01 工作日志

- 新增需求：房间内"玩家不可移动"区域（Pattern A 碰撞挡墙）。用户确认要手动/数据驱动添加，不要写进 Game.tscn（运行时被 RoomManager 重建会丢）。
- 坐标系（来自 RoomManager.gd）：房间 W=880 H=500，中心原点(0,0)，X∈[-440,440] Y∈[-250,250]，Y 向下正。边界墙已用 StaticBody2D(collision_layer=16)+_add_wall 建四面。内部禁区拟复用同一 StaticBody2D/layer16，新增房间数据 `blocked` 数组（[cx,cy,w,h] 列表），在 `_build_walls()` 里逐条 `_add_wall()`，进房自动重建。
- 沟通协议：用户用 ASCII 草图（#=挡 ·=可走 P=出生 D=门，我按比例缩放到房间）或矩形列表（房间局部坐标 [cx,cy,w,h]）描述禁区位置；需同时指定房间 rid。待用户给具体 spec 后实现。
