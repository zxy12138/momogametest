# 2026-08-06 工作日志

## 武器系统重构（8 把武器 + 悬浮三武器）
- 读取开发日志武器相关条目（CHANGELOG 武器节点 / 数据驱动 / 选武器流程）。
- 切图：`assets/weapons/icons/Allweapons.png`(1408×768, 4×2 网格, 每格 352×384) → 自动裁边生成 8 把武器图标 `weapon_{staff,sword,scythe,bow,hammer,whip,spear,axe}.png`（约 250–320px）。顺序：第1行 法杖/剑/镰刀/弓，第2行 锤/鞭/枪/斧。
- 生成占位特效 9 张：`assets/weapons/fx/weapon_fx_{staff,bow,axe,sword,scythe,hammer,whip,spear}.png` + `weapon_fx_hit.png`（PIL 绘制，后期替换真美术）。脚本 `tools/gen_weapon_assets.py`（venv 已建：Pillow 12.3.0）。
- 重写 `src/data/Weapons.gd`：9+9 旧数据 → 8 把基础武器（无升阶），新增 `atk` 行为标签（ranged_bolt / melee_arc / melee_ring / ranged_arrow / melee_slam / melee_fan / melee_line / ranged_spin）、`fx` 路径、`pick_three()`；保留 `STARTERS`/`SWAP_POOL`/`get_weapon`/`can_upgrade` 兼容 `Game.gd`。
- 数值表已设计（DPS 20–50 平衡），待用户确认后进入「悬浮三武器」系统实现。

## 待确认（高影响设计）
- 用户已答复：仅主武器出手，另 2 把悬浮展示；地面拾取替换主武器槽。

## 悬浮三武器系统已实现（2026-08-06 续）
- 新 `src/weapons/WeaponSystem.gd`（替换 Player.tscn 的 "Weapon" 节点脚本，零改 Player.gd）：3 槽 loadout 环绕玩家，主武器按 atk 行为攻击（远程弹体 / 近战 360/fan/line/slam+特效），Q/滚轮切换主武器。
- `GameManager.loadout` + `reset_run_loadout`；`Game.gd` 起始灌 loadout、拾取替换主武器槽、每新房掉 1 把 SWAP_POOL 地面武器；`WeaponSelect` 随机 3 张卡；`project.godot` 加 `switch_weapon`(Q+滚轮)；`Projectile` 加 `spin`（飞斧自转）。
- 静态核对一致（GameManager 的 crit_dmg/skill_cd_mult/roll_crit/fx/load_tex 均存在）。旧 `Weapon.gd` 成孤儿脚本（保留）。
