# 2026-08-16 工作日志

## 门判定框显示修复 + 去黑边算法 v2 重写

### 门判定框（编辑器 + 运行期）
- **坑**：project.godot 残留 `hide_door_visuals=true` → 编辑器/运行期都读到 true 而跳过绘制，表现为"无论勾不勾都不显示"。
- **坑**：勾选框 `_on_hide_door_visual` 只写设置、不重绘场景手柄 → 勾选后编辑器画面不刷新。
- **修复**：①残留值归 false；②勾选回调里 `_collect_door(root)` 收集所有 DoorHandle 逐个 `call("_redraw")`（复用 `_on_hide_blocked` 的模式）。
- **约定**：门判定框隐藏开关 = `BlockedHandle.HIDE_DOOR_VISUAL_SETTING="momogame/hide_door_visuals"`（放 BlockedHandle 避免 DoorHandle↔RoomManager 循环依赖）；DoorHandle._redraw 和 RoomManager._build_doors 都读 `BlockedHandle.is_door_visual_hidden()`。

### 去黑边 color_bleed.py（v2）
- **坑**：v1 用「邻居颜色取平均」扩散 64 轮，把门框深色+门洞亮色混成灰(127)、RGB 标准差 83 = 彩虹/杂色边。
- **正确算法**：BFS 逐轮向 上/下/左/右 **拷贝最近不透明像素的颜色（只拷贝不平均）**，auto-terminate（`if not changed: break`）。
- **验证口径**：羽化区（0<alpha<255）平均 RGB 应≈不透明区颜色、标准差应小（~20 是正常纹理，~83 是彩虹，≈0 是黑边）。
- 全图 3120×6540 约 1 分钟；单帧 390×436 约 0.3s。
- **后续**：用户要求门框回退黑边版（去黑边结果不采用），`S_001_1_opendoor.png` 已恢复黑边版、`.bak` 已清理。color_bleed.py 保留备后续素材用。

### 判定框可拖拽调整大小（DoorSizeHandle）
- **新增** `src/rooms/handles/DoorSizeHandle.gd`（class_name DoorSizeHandle，黄色小方块手柄）。
- DoorHandle 加 `_size_handle`：`_ensure_size_handle()` 创建并设 owner（才能编辑器选中拖动）；`_ready` 复位到 (door_size/2, door_size/2)；`_process` 读手柄位置算 `door_size`；`_redraw` 跳过手柄保留。
- **坑**：@tool 可拖拽控制点**别在 _redraw/_process 里吸附回写其位置**（和编辑器拖动打架抖动），让控制点自由、读位置映射目标值。
- **修正（门是长方形）**：`door_size` 从 float（正方形）改为 **Vector2（矩形 宽×高）**；黄点 x=宽/2、y=高/2，拖 x 改宽、拖 y 改高；RoomManager 碰撞 `RectangleShape2D(dsize)` 矩形；`_add_door_visual(size: Vector2)` 画矩形。
- **坑**：@export 属性改类型后旧 .tscn 存的值不兼容，须同步改 tscn 值格式（`door_size = 62.0` → `Vector2(62, 62)`）。

### 受击红光移除 + 电梯动画 + 近战攻击范围
- **受击红光**：`Player.hit_by` 里 `_sprite.modulate = Color(1,0.4,0.4)`（角色变红）即"受击闪红"，用户不要 → 去掉，只留描边提亮。
- **电梯动画 dianti**：RoomManager `_find_dianti()`（stop+frame=0+关循环）+ `_process` 检测 `get_tree().get_nodes_in_group("enemy").is_empty()` → `play("default")`（不循环定格末帧）。"怪清空"用 group 检测（单房间实例，group 全局即当前房；敌人 queue_free 自动移出 group）。
- **近战攻击范围**：对着攻击动画实际臂展调 atk_range（bbox 臂展 × sprite scale），别用统一默认 80。闹钟怪臂展 ~22 世界单位 → atk_range 40。

### 开门时机重构 + 禁区隐藏持久化 + 素材库 json
- **门生效逻辑**：`GameManager.door_opened`（瞬态，reset_run/apply_death 清空）记录是否已开门；`RoomManager`：`play_open_door` 播放即 `_enable_doors`（不等播完）+ 设 door_opened；`_find_door_anim` 若 door_opened → 最后一帧 + 生效（切房回来"已开"）；`_process` 检测 `group "enemy"` 空 → 无开门动画的门生效（战斗房怪死才开门）+ dianti 播放。`_enable_doors` 用 `_doors_enabled` 防重。
- **禁区隐藏持久化**：`BlockedHandle._redraw` 判断必须 `if s_hidden_all or is_visual_hidden():`（is_visual_hidden 读 project.godot），否则关软件重开静态变量重置 → 禁区又显示。
- **素材库 json**：目录多 json 时按图片 basename 匹配同名 json（`_find_atlas(dir, img_basename)`），别取第一个（否则选中文图读到英文 json 的帧宽）。Godot 4.7 中文路径/中文文件名 DirAccess/FileAccess/JSON 都正常，编码不是坑。

### 怪物方形判定框 + 开门 + 驿站 + 阴影描边（大改）
- **怪物方形判定框**：`Enemy.gd` 加 `const CB` 表（16 怪 Vector4 宽高偏移），`_apply_collision_box()` 用 RectangleShape2D 替换 CircleShape2D（碰撞+受击框）；`WeaponSystem._enemy_radius` 兼容矩形（长边一半）。**坑**：怪物帧宽统一 128（fw=128），帧数不同（10/8/6/4/3），别假设 8 帧；瘦长身体用圆形会超宽，方形贴合 bbox 最准。工具 `tools/analyze_enemy_collision.py`。
- **f1_r3/r4 开门**：`GameManager.r34_opened`（key=rid）；`RoomManager._find_enter_door_anim()` 识别 `S_001_3or4_left/right`，首次隐藏 momo 播动画、播完显示，后续停最后一帧。Game.set_player_visible() 控制 momo 显隐。（2026-08-16 03:46 重构为演出状态机，见下）
- **驿站**：`InnHandle` 继承 BlockedHandle（复用多边形顶点，蓝色可视化）；RoomManager._build_inn 生成 Area2D → Game.set_inn_nearby → 按 F ConfirmationDialog 回满血。**坑**：`_collect_handles` 里 `is InnHandle` 要在 `is BlockedHandle` 之前（子类优先）。
- **阴影+描边**：`_add_shadow()` 脚下椭圆 Polygon2D；Player outline_color 改黑；怪物 `_add_outline()` 用 `tools/gen_enemy_outline.py` 批量生成 `_outline.png`（MaxFilter 扩张1px-原 alpha=描边环）+ `_sync_outline()` 每帧同步。**坑**：--script 模式不加载 autoload，验证语法用 --editor（"Identifier not found: GameManager" 是假错误）。

### f1_r3/r4 进入关卡演出（演出状态机）
- **需求**：首次进入 f1_r3/r4 做演出——门开完 → 视角下移看怪（怪暂停）→ 视角回中 → momo 出现关卡开始；再次进入保持最后1帧无演出。
- **架构**：演出状态机放 **Game**（相机/玩家/输入锁都在 Game），RoomManager 只暴露数据——`get_enter_door_anim()`（S_001_3or4 节点）+ `get_enemy_center_local()`（EnemyHandle 位置平均=视角下移目标）。RoomManager._find_enter_door_anim 只「停帧+关循环」（首次停第一帧、再次停最后一帧），不再自己播放/隐藏 momo。
- **敌人暂停**：全局 `GameManager.cutscene_frozen`，`Enemy._physics_process` 读到 `velocity=ZERO; return`（静止展示）。比遍历 group 设状态简洁。
- **相机平移演出**：固定视角的相机，在 `_update_camera` 的 focus 上叠加 `_cam_focus_offset`，用 `smoothstep` 逐帧 lerp（下移0.8s → 停留0.7s → 回中0.8s），不动 Camera2D。
- **状态机推进**：`_check_enter_intro()`（_swap 里 setup 后，首次且 r34_opened 未记录 → 锁输入+cutscene_frozen+隐藏momo+play门动画）→ `_physics_process` 里 `_tick_intro`（match phase 1门动画等`is_playing()`结束→2下移→3停留→4回中→5完成）→ `_finish_intro`（解冻+解锁+momo显示）。
- **f1_r2 电梯回来保持最后帧**：`GameManager.dianti_done`（key=rid）；`_process` 怪死光播放 dianti 时置 true；`_find_dianti` 读它——已播过停最后一帧、未播停第一帧。
- **坑（R4有演出R3没有）**：f1_r3 的 `S_001_3or4_left` 套在 `Polygon2D(clip_children)` 下（非直接子节点），`get_children()` 找不到 → `_find_enter_door_anim` 用 `find_children("S_001_3or4_*", "AnimatedSprite2D", true, false)` 递归查找兼容两种结构。**场景节点不一定是根直接子节点，找节点用 find_children 别假设 get_children() 直达。**

### mp4_to_ogv_gui.py 指向用户 ffmpeg（ffprobe 补齐）
- **需求**：`mp4_to_ogv_gui.py` 硬编码的旧路径 `E:\python project\ffmpeg\bin\` 失效，改为指向用户 venv `D:\Pythonproject\.venv\Scripts\`。
- **探测**：`D:\Pythonproject\.venv\Scripts\ffmpeg.exe` 已存在（gyan.dev 7.1 essentials，含 libtheora），但**缺 ffprobe.exe**（imageio-ffmpeg 只带 ffmpeg）。
- **补 ffprobe**：托管 Python 装 `static-ffmpeg` 包（清华镜像），二进制走 GitHub 下载成功 → `...\envs\default\Lib\site-packages\static_ffmpeg\bin\win32\ffprobe.exe`（8.0.1，含 libtheora），`cp` 到用户 venv。
- **验证**：ffmpeg 7.1 + ffprobe 8.0.1 均 `-version` 正常；脚本 `py_compile` 通过；ffprobe 实测读 `assets/Anime/A_001.mp4` 时长 15.07s 成功。
- **注意**：ffprobe 比 ffmpeg 版本新（8.0.1 vs 7.1），只读时长不影响；跑 GUI 脚本仍用 `D:\Pythonproject\pythonProject\.venv\Scripts\python.exe`（用户 Python 3.14 本身 numpy 坏，但 ffmpeg/ffprobe 是独立 exe 不受影响）。

### f1_r7 背景换 S_001_7_All.ogv + 分段播放（boss 房演出）
- **需求**：f1_r7 背景由静态图 S_001_7.png 换成动态视频 S_001_7_All.ogv（8.08s 1280×720@60fps，前 4s boss 出场 + 后 4s boss 死后场景）；momo 进房自动走向 boss → boss 出现 → 视频播 4s 暂停 → 弥绘对话 → 对战；boss 死亡动画后 → 视频续播 4s → 固定末帧（切房回来保持）。
- **素材确认**：`assets/tiles/S_001_7_All.ogv` 已存在；**Godot 4 Theora 视频无需 .import 转码**（.godot/imported 无 ogv 缓存也正常），`ResourceLoader.exists=true`、`load` 得 `VideoStreamTheora`、可赋给 `VideoStreamPlayer.stream`。
- **LevelData**：`TILES[1]["r7"]` 改 `"S_001_7_All"` → `tile_path` 自动优先 ogv → RoomManager `_build_floor` 走 `_add_video_floor`。
- **RoomManager 分段控制**：`_add_video_floor` 对 f1_r7（`_rid=="r7" and _layer==1`）→ `loop=false` + 存 `_bg_video`；tree_entered 回调 play 后：已播完（`r7_video_done`）→ `set_stream_position(999999)` clamp 末帧 + paused；未播完 → paused 停第一帧。状态 `_bg_phase`：0=停帧 1=第一段播 2=暂停4s 3=第二段播 4=完成。`play_bg_segment1()`/`resume_bg_segment2()`/`is_bg_segment1_paused()`/`is_bg_video_done()`/`_seek_bg_end()`；`_process` 检测 `stream_position>=4` → paused、`>=8` → `_seek_bg_end`（写 `r7_video_done`）。**坑：loop 必须在 play() 前设（tree_entered 回调里 play 时再改无效）→ add_child 前设**。
- **boss 延迟生成**：`_spawn_content` 对 f1_r7 未清 → 不立即 spawn，存 `_pending_boss`（[bid,pos]），`spawn_boss_now()`（Game 演出到「boss 出现」时调）= 生成 boss + `play_bg_segment1()`；`get_boss_local()` 返回第一个 BossHandle 位置供 momo 走位目标。
- **Player auto_walk**：`auto_walk_to(target,speed)`/`is_auto_walking()`；`_physics_process` 在 `_is_gm_locked()` **之前**处理自动行走（演出期间 input_locked=true 但 momo 必须能走），到位 velocity=ZERO 播 idle。
- **Game 演出状态机**（f1_r7 首次进入）：`_check_boss_cutscene`（锁输入+cutscene_frozen+momo auto_walk 到 boss 下方 130px）→ `_tick_boss_cutscene` phase 1 momo 到位 → phase 2 `spawn_boss_now()` → 等 `is_bg_segment1_paused` → phase 3 弥绘对话 → finished 回调 `_finish_boss_cutscene`（解冻+解锁）。`on_boss_defeated`：layer==1 → 延迟 1.6s（死亡动画）→ `resume_bg_segment2()`。
- **GameManager**：`r7_video_done := {}`（key=rid，持久化），reset_run/apply_death 清空。
- **坑（@tool 预览禁 autoload）**：RoomManager 是 @tool，编辑器打开 f1_r7 时 `_ready`→setup→`_add_video_floor` 的 lambda 访问 `GameManager.r7_video_done` 崩（placeholder 无此字段）→ tree_entered 回调里 `if Engine.is_editor_hint(): return` 守卫。
- **验证**：无头 --editor 编译零错误；`tile_path(1,"r7")=res://assets/tiles/S_001_7_All.ogv` + load 得 VideoStreamTheora；VideoStreamPlayer `paused`/`stream_position`/`loop` 属性 + `set_stream_position` 均可用（无头 is_playing=false 是 Theora 无渲染上下文，正常）。
- **待用户操作**：在 f1_r7.tscn 编辑器里摆放 BossHandle（b_director），否则 `_pending_boss` 空 → 演出只播视频无 boss。

### 三 Boss 双形态接入（2026-08-16 17:30）
- **需求**：3 boss × 2 形态 + 变身动画，替换旧 boss。素材 `assets/sprites/bosses/{layer}/F{x}_B_*`。
- **素材核对**（用户帧数全部吻合）：F1_B_001 公交(上6/下6/右5/触手2/冲撞4+4两行合一)、F1_B_002 机器人(下8/上8/右8/死8/铁块8/下砸8/待机8/弹道2随机)；F2_B_001 昆虫僵尸(下8/上8/右8/抓人8/毒液8/弹道1/待机8)、F2_B_002 完全昆虫(待机8/上8/下8/右8/死8/踢人8/光束7/弹道1)；F3_B_001 人形(下12/上12/右12/待机10/拍巴掌10，无远程无死亡)、F3_B_002 电脑机(待机10/下10/上10/右10/砸地10/光弹6/死10/弹道3随机)。变身动画 F*_B_00{1}Anime：F1 120帧/3.97s、F2 114帧/3.77s、F3 146帧/4.83s（1280×720/帧，json 帧坐标）。
- **坑（行带合并）**：F2_B_002 行5(死亡599~727)与行6(踢人728~873)间隙仅1px，自动检测合并成 599~872 → `ROW_OVERRIDES` 提供完整 8 行 y 映射。
- **坑（跨行合并）**：F1_B_001 行5+行6 = 冲撞 8 帧（两行各 4 帧拼一个动作）→ `MERGE_CHARGE`。
- **切片**：`tools/slice_boss.py`（帧格 192，输出 {fid}_{anim}.png + *_left 镜像 + proj + transform 变身条）。
- **Enemies.gd BOSS 表重写**：`b_bus`(hp1=250,hp2=400)/`b_bug`(300/500)/`b_pc`(500/900)；每 boss `form1`/`form2`/`transform`/`transform_frames`/`transform_fps`/`proj`/`proj_frames`/`proj_random`/`cb`/`scale`。常量 B→B1/B2/B3（按层目录）。
- **Boss.gd 双形态状态机**：`_enter_form(1/2)` 切数据+动画+血量+碰撞+阴影；`_start_transform()` 形态1打空→无敌+播 transform 动画（SpriteFrames，120/114/146帧）→`create_timer(dur)` 切形态2满血；`_die` 只有形态2 走（形态1 无死亡动画直接变身）。行为 `_tick_form_behavior`：charger(公交冲撞)/patrol(人形近战)/hybrid(昆虫·机器人·电脑机远近混合)。**覆盖 `_tick_status`**（父类每帧用 `_data.get("speed")` 强制回退顶层 speed，Boss 需从 `_form_data.speed` 读）。
- **坑（覆盖签名）**：`_melee_attack(anim_base: String = "attack")` 必须带默认参数（父类签名 `= "attack"`），否则 Parse Error "signature doesn't match"。`_kb_t`/`_kb_dir` 父类已有，勿重复声明。
- **Projectile**：加 `proj_random` 字段（true 时 `_ready` 随机选帧、不轮播）——boss 铁块/光弹"随机取1帧"。
- **BossHandle**：BIDS 改 `["b_bus","b_bug","b_pc"]` + `@export_enum("b_bus:梦魇公车",...)`；`_redraw` 适配新 BOSS 表（form1.idle 首帧 + "形态1→变身→形态2" 标签）。
- **场景**：f1_r7 已有 Handle（type 默认0=b_bus，位置95,-44）；f2_r7 注入 Boss 节点（type=1=b_bug，0,-60）；f3_r7 原 type=2 恰好=b_pc。LevelData r7 boss 字段同步。
- **验证**：无头 --editor 编译零错误；数据层校验（SceneTree -s 不加载 autoload，只查 BOSS 表字段/素材存在性）；64 素材路径 Python 校验全存在。
- **坑（-s 测试限制）**：Boss.gd 引用 GameManager（autoload），`-s` 模式编译报 "Identifier not found: GameManager" 是环境限制非代码错；运行时验证用 --editor --quit 编译 + 数据层静态校验代替。

### 4 项修复（2026-08-16 18:30）：Boss体型/切片/驿站插件/f1_r7黑屏
- **Boss 体型太小**：`Boss.gd` base scale 0.45→`Vector2(sc,sc)`（帧格 192 全尺寸显示），形态 scale 提到 1.0~1.45（公交 1.45≈200px 高，其余 127~172px）；cb 同步放大。玩家参考 58px、旧 boss 225px。
- **切片改透明间隙**：`slice_boss.py` 的 `make_strip` 重写为「内容带（透明间隙）优先：带数==帧数直接用；否则帧间距中位数均匀切」（复用 slice_enemy_rows v3 策略）；弹道行加 `PROJ_FRAMES` 表（F3_B_002 光弹 3 帧、F1_B_002 铁块 2 帧——内容带含 1px 碎片检测 4 带需按用户帧数切）。重切后全部帧数与用户说明一致。
- **插件缺驿站按钮**：`RoomDockPanel.gd` 加「+ 驿站」按钮 + `_add_inn()` + 计数（n_inn）+ 命名（Inn 在 Blocked 前判断——InnHandle 继承 BlockedHandle）。运行期链路（RoomManager._build_inn → Game.set_inn_nearby → 按 F 回满血）已存在，只缺面板入口。
- **f1_r7 黑屏 + 门判定框**：黑屏=`_add_video_floor` 分段模式 `play()` 后立即 `paused=true`，Theora 首帧未解码 → 画面黑。修复：`await get_tree().process_frame` ×2 等首帧渲染再 pause（固定显示第一帧）。门判定框：f1_r7.tscn 补 `Door_r6`（DoorHandle target=r6，位置 -424,152，尺寸 28×182，SizeHandle 可拖）——来时门判定框；NextDoor 已有（boss 死后 enable_next_door）。
- **坑**：python 检查 `'DoorHandle.gd' in c` 会被 `NextDoorHandle.gd` 子串误判 → 用正则精确匹配 `path=".../DoorHandle\.gd"`。

### 3 项修复（2026-08-16 19:20）：NextDoor判定框/演出顺序/驿站提示位置
- **NextDoor 判定框看不到**：`NextDoorHandle._redraw` 原来只画门框贴图+「↑下一层」标签，无判定框。加 `@export door_size: Vector2 = (48,48)` + 金色半透明矩形判定框（Polygon2D+Line2D，复用 DoorHandle 样式）；`RoomManager._build_next_door` 的碰撞框 48×48 改用 `h.door_size`（运行期/编辑器所见一致）。f1_r7 NextDoor 在 (316,-144)。
- **演出顺序重排（v5.2）**：用户明确顺序=momo 斜着往上走到地图中间 → 视频播 4s → boss 出现 → 对话(boss 静止) → 结束开打。改：①`spawn_boss_now()` 拆掉内置 `play_bg_segment1()`（boss 生成与视频启动解耦，视频时序由 Game 控制）；②`Game._check_boss_cutscene` 目标从 `get_boss_local()+(0,130)`（boss 面前）改为**地图中间偏左上** `_room.global_position + Vector2(-60,-30)`（f1_r7 出生点默认 (0,190) 底部中央 → 斜上走到中央，boss 在右上 (242,-80) 对峙）；③`_tick_boss_cutscene` 重排：phase1 走到位→`play_bg_segment1`；phase2 等 `is_bg_segment1_paused`→`spawn_boss_now`+`_play_boss_intro_dialogue`（boss 刚生成即 cutscene_frozen 静止）；phase3 对话中。
- **驿站 F 提示位置**：原来 `Game.set_inn_nearby(true)` 用 `toast("按 F 回满血")`（屏幕提示，非驿站上方）。改：`RoomManager._build_inn` 给每个 inn Area2D 挂「按 F 回血」Label（position (-44,-54) 驿站上方，z_index 98，初始隐藏），进出判定框用 lambda 闭包控制 label.visible（body_entered/exited 直接连 lambda，删掉 `_on_inn_entered/_on_inn_exited` 两个函数）；Game.set_inn_nearby 只维护 `_inn_nearby` 状态不再 toast。
- **验证**：无头 --editor 编译零脚本错误；静态校验 spawn_boss_now 无 play_bg_segment1、Game 引用齐全、驿站 Label 存在、NextDoor door_size 存在；enable_next_door 调用链（Game:382 → RoomManager:763）完整。

### 7 项需求（2026-08-16 20:30）：F11秒杀/r6视频/公交缩小/r7禁区动态/去地图名/动画速度/攻击间隔
- **F11 秒杀**：`Enemy.kill_now()`（hp=0 → `_die(false)`）+ `Boss.kill_now()` 覆盖（形态1 先 `_enter_form(2,false)` 跳过变身动画直接切形态2 再死——触发 on_boss_defeated 通关流程）；`Game._unhandled_input` KEY_F11 → `_kill_all_enemies()` 遍历 group "enemy" call kill_now + toast 计数。
- **f1_r6 视频背景**：`LevelData.TILES[1]["r6"]="S_001_6_All"`（走 `_add_video_floor` 普通循环分支 loop=true 一直循环；已验证 tile_path→ogv→VideoStreamTheora）。
- **公交缩小**：`Enemies.gd` b_bus form1 `scale=1.45→1.2`（显示 ~200px→165px 高）。
- **f1_r7 Blocked_2/3 动态禁区**：`RoomManager._build_blocked` 特判（`_rid=="r7" and _layer==1 and h.name in ["Blocked_2","Blocked_3"]`）→ `_boss_blocks` 数组 + 初始禁用（CollisionShape2D/CollisionPolygon2D.disabled=true + Polygon2D.visible=false）；`set_boss_arena_blocks(v)` 切换；Game 演出 phase2 调 true（boss 出现）、`on_boss_defeated` 调 false + `GameManager.r7_blocks_gone[rid]=true` 持久化（reset_run/apply_death 清空）；回来时 `_build_blocked` 读持久化直接跳过构建（保持消失）。禁区 StaticBody2D collision_layer=16。
- **去地图房间名**：`MapUI.gd` CURRENT 房间删「r1 · 战斗」标签（只留指针动画）、VISITED/BOSS_CLEARED 按钮 `text=""`（保留传送点击）。
- **动画速度调慢（防鬼畜）**：`Enemy.gd` anim_fps idle 8→6 / walk 12→8 / attack·melee 14→10 / dead 12→8（整体降 ~30%）。
- **攻击间隔调大**：初始冷却 `randf_range(0.3,1.5)→randf_range(1.0,2.5)`（进场先观察）；行为冷却默认值 1.5/2.2/1.2/1.6/1.2→2.6/3.2/2.2/2.8/2.2；`Enemies.gd` 全部 atk_cd/melee_cd ×1.7 取整（1.2→2.0 … 1.9→3.2，28 处 python 正则替换，注意 `(?!\d)` 防 1.25 误配）。
- **验证**：无头 --editor 编译零错误；18 项静态校验全 PASS；r6 ogv 加载 OK。

### 5 项修复（2026-08-16 21:30）：S_001_7报错/场景文字/章节黑屏/开门动画/武器不重复
- **S_001_7_All 三连报错**：根因=Artssucai 源目录有 **215MB 的 S_001_7_All.png**（含 .png.import），Godot 全目录扫描导入失败（webp 解码 → ctex 失败 → 回退源文件再失败）。修：删 `Artssucai/dongtaichangjing/S_001_7/S_001_7_All.png` + `.png.import` + `.godot/imported/S_001_7_All.png-*.ctex/md5` 失败缓存 + `src/rooms/scenes/S_001_7_All.ogv` 无用副本（无人引用）。**教训：项目内大 PNG 素材库（Artssucai 215MB 级）会被 Godot 全目录扫描，超大/异常图会导入报错——用 .gdignore 或移出项目，只留 ogv/json。**
- **场景文字 vs 地图文字（用户澄清）**：用户要的是**去场景文字**（编辑器/运行期手柄标签），不是地图文字。①MapUI 还原（CURRENT「r1 · 战斗」标签 + VISITED 按钮文字恢复）；②去手柄文字标签：DoorHandle（箭头+类型主标签、→r3 副标签）、NextDoorHandle（↑下一层）、BossHandle（怪名+形态1→变身→形态2）、EnemyHandle（怪名）、WeaponHandle（武器名）、DecorationHandle（装饰:xx）——全删，只留精灵/判定框可视化；③RoomManager 运行期 NextDoor「↑ 下一层」标签也删；驿站「按 F 回血」保留（交互提示）。
- **第二/三关章节黑屏**：`Game._go_next_layer` 加 `_show_chapter_card(next)`——独立 CanvasLayer(layer 120) 黑底 ColorRect 全屏 + 居中 72px「第二章/第三章」大字，渐显 0.45s → 保持 1.2s → 渐隐 0.45s → queue_free。
- **f1_r1 开门动画不播**：用户把 S_001_1_opendoor 套进 Polygon2D(clip_children) 蒙版下 → `_find_door_anim` 的 `get_node_or_null("S_001_1_opendoor")`（直接子节点）找不到 → _door_anim=null 不播。改 `find_children("S_001_1_opendoor","AnimatedSprite2D",true,false)` 递归查找（同 f1_r3/r4 坑）。
- **选武器不刷同款**：`_spawn_weapons` 随机池排除 `GameManager.weapon_id`（`_adv` 后缀归一化）+ `loadout` 全部槽位再抽；池空兜底全量。

### 标题界面场景化改造（2026-08-16 21:40）
- **需求**：标题循环.ogv 循环背景 + 像素风标题（上中文下英文）+ 按任意键继续 + 新游戏/继续/设置/退出 + 设置调音量 + 做成 tscn 直接可见。
- **Main.tscn 重写为完整场景化节点树（24 节点）**：VideoBG（VideoStreamPlayer：`标题循环.ogv` 1280×720@60fps 8s 纯视频无音轨，autoplay+loop+expand+audio_track=-1 全屏）+ Overlay（0.35 黑压暗）+ TitleShadow/TitleCN（76px「梦境逐影」硬阴影像素风：黑字偏移7px+白字描边16）+ TitleEN（30px「DREAM CHASER」金描边）+ PressAny（闪烁，BlinkTimer 0.55s）+ MenuRoot(CenterContainer)→MenuPanel(VBox, 4 按钮 240×52) + SettingsWrap→SettingsPanel(面板 360×260：主音量 HSlider + 值 + 提示 + 返回)。
- **像素按钮**：UI-022/023/024 九宫格（80×18，texture_margin 16/8）做成 3 个 StyleBoxTexture + Theme（`Button/styles/normal|hover|pressed|focus(Empty)` + 字体白/描边4/18px），Main.theme 全局生效，编辑器即可见。
- **Main.gd**：`_input` 任意键/点击 → `_show_menu`（PressAny 隐藏+菜单淡入）；音量滑块 → `AudioServer.set_bus_volume_db(0, linear_to_db(v/100))` + `set_bus_mute(0, v<=0)`，持久化 `ProjectSettings.set_setting("game/master_volume", v)+save()`（默认 80）；继续按钮 `disabled = not SaveManager.has_save()`；新游戏/继续/退出逻辑保留（Intro.tscn / Game.tscn）。
- **坑**：项目无 BGM 播放器/无像素 TTF（UI-082 是 12×12 单字符非字体）；音量只能接 AudioServer master 总线（未来音频自动受控）；ogv 无音轨 → audio_track=-1。
- **验证**：无头编译零错误；场景 24 节点结构+8 项配置 PASS；`标题循环.ogv` load→VideoStreamTheora OK。

### ESC 菜单改造 + 多槽存档 + 返回标题修复（2026-08-16 22:00）
- **SaveManager 多槽化**：`save_game/load_game/has_save/clear_save` 全部加 `slot: int = 1` 参数（默认 1 兼容旧调用，自动存档仍写槽 1），路径 `user://save_{slot}.json`；新增 `slot_desc(slot)`（层名+时间戳 MM-DD HH:MM）；存档加 `saved_at` 时间戳。
- **Main 继续游戏弹存档选择**：Main.tscn 加 SaveWrap→SavePanel（3 槽按钮 + 提示 + 返回）；Main.gd 继续按钮 → `_open_save_select`（刷新槽显示：有档=层+时间、空槽 disabled）→ 点槽 `_continue_slot(slot)` → load_game(slot)+apply_to_state+进 Game。
- **ESC 菜单全暂停 + 设置 + 存档 + 重开本小关**：`get_tree().paused=true` 全树暂停（画面全冻结），菜单面板 process_mode=ALWAYS 交互；**Game.tscn 根 process_mode=3(ALWAYS) + $World process_mode=2(PAUSABLE)**（Game 暂停时仍收 ESC，世界内容全停）；`_physics_process` 开头 `if get_tree().paused: return`；UILayer(运行时 CanvasLayer) process_mode=ALWAYS。菜单按钮：继续/存档(3槽手动保存)/设置(音量滑块→master 总线+ProjectSettings 持久化)/**重新开始本小关**（`_restart_room`：回满血蓝+`transition_to(current_room, true)` 重载当前房，普通房怪刷新）/返回主菜单。
- **返回标题 bug 修复**：`_return_menu` 清瞬态（input_locked/cutscene_frozen/weak_window/game_completed/prologue_pending/prologue_dialog_active + paused=false）；**Main._ready 加防御清理**（覆盖 DeathCG/Birthday/WeaponSelect 直接 change_scene 不清状态的路径——死亡后 input_locked 残留会卡死再进游戏的玩家）。
- **坑**：①ESC 全暂停必须处理 process_mode 链——Game 根 ALWAYS（收输入）+ World PAUSABLE（世界停），不能全局 ALWAYS（敌人/弹道会跟着跑）；②`get_tree().paused` 下 VideoStreamPlayer 背景视频也暂停（恢复继续）；③返回 Main 的路径有 4 条（ESC/死亡/生日/选武器返回），统一在 Main._ready 防御清理最稳。
- **验证**：无头 --editor 编译零错误；20 项静态校验全 PASS（-s 测试受 autoload 限制跳过，用编译+静态代替）。

### 紧急修复：进游戏全暂停（2026-08-16 22:10）
- **症状**：一进游戏所有东西静止，只有按 ESC（暂停时）世界才动——完全写反。
- **根因**：**Godot 4 process_mode 枚举：0=INHERIT、1=PAUSABLE、2=WHEN_PAUSED、3=ALWAYS、4=DISABLED**。我在 Game.tscn 给 $World 写 `process_mode = 2`（=WHEN_PAUSED 只在暂停时运行）→ 平时世界静止、暂停时反而运行。Game 根 3(ALWAYS) 是对的。
- **修复**：Game.tscn `$World process_mode = 2 → 1`（PAUSABLE：平时运行、暂停时停）。最终：Game 根 ALWAYS(3) + World PAUSABLE(1) + UILayer/菜单 ALWAYS。
- **坑**：tscn 里 process_mode 是**数字枚举**，1 才是 PAUSABLE！写 2 就变成 WHEN_PAUSED（写反）。gd 里用 `Node.PROCESS_MODE_*` 常量名不会错，但手写 tscn 数字极易踩。项目里仅 Game.tscn 用了 process_mode（其余场景无暂停需求）。

### 紧急修复2：进下一关卡死——CanvasLayer 无 modulate（2026-08-16 22:15）
- **症状**：f1_r7 打完 boss 进第二关时 `_show_chapter_card: Invalid access to property 'modulate' on CanvasLayer` → 卡死。
- **根因**：章节黑屏卡片把渐变动画挂在 **CanvasLayer 上（`cl.modulate`）——CanvasLayer 不是 CanvasItem，没有 modulate 属性**。
- **修复**：`_show_chapter_card` 里 CanvasLayer 下加一个全屏 `Control` 容器（root），bg+Label 挂 root 下，对 **root.modulate** 做渐显/渐隐；结束时 queue_free CanvasLayer。
- **坑**：CanvasLayer（layer 容器）与 CanvasItem（Control/Node2D，有 modulate）是不同体系，渐变/透明度只能挂 CanvasItem。

### 5 项修复+演出（2026-08-16 22:40）：相机残留/章节字/场景房间名/通关选项/完整演出
- **返回标题画面放大**：Game 每帧写 `viewport.canvas_transform` 缩放跟随，返回 Main 后残留（change_scene 不重置）→ Main UI 被放大。修：Main._ready `get_viewport().set_canvas_transform(Transform2D.IDENTITY)`。
- **章节字显示不全**：`_show_chapter_card` 的 Label 用 `set_anchors_preset(PRESET_CENTER)`（size=0）→ 文字位置偏/显示不全。改 `PRESET_FULL_RECT` + horizontal/vertical_alignment=CENTER。
- **场景房间名**：真正来源是 **Layer1/2/3.tscn 的 `r{id}/Ghost/Title` Label（"r1 · 起点"等 22 个）**（之前误改 MapUI）。Layer.gd 隐藏 ghost 的 `visible=false` 对 Control 子节点未生效。修：python 删 3 个 tscn 全部 Title 节点（22 个）。
- **通关两选项**：`_show_completion_overlay` 的"按 ESC 返回"改为「返回主菜单」+「继续游玩」两按钮；`_continue_playing()` 关覆盖层+解锁输入（留世界自由探索，ESC 恢复暂停菜单）。
- **完整演出（v4.0 §5.3/5.4）**：
  - 新立绘：Artssucai/Live2d 的 momo_move.png（弥绘感动）+ zhujue_happy.png（粉丝清醒版）→ 复制到 assets/Live2d/。
  - f2/f3 Boss 前对话：`_play_boss_intro_dialogue` 按层选台词（f2「好挤…赶在梦醒前过去」、f3 本体梦呓「谢谢你…支持我直播」+弥绘回应）；`_swap` boss 分支扩展 `not boss_cleared.get(layer_index)` → f2/f3 进房直接播对话。
  - 击败过场：`_play_boss_defeat_dialogue(layer)`（f1「站牌碎了」/f2「车厢散成光」），on_boss_defeated 延迟 1.6s 播，结束 enable_next_door。
  - **Epilogue 花海三段式**：Epilogue.tscn 背景改 S_004_1.jpg（TextureRect expand+cover）；Epilogue.gd 用 GalgameDialog 播三段（灵魂融合旁白→花海重逢粉丝zhujue_happy+弥绘momo_move→生日蛋糕反转），播完（或 ESC 跳过，GalgameDialog 自带）change Game 通关态。
- **坑**：①viewport.canvas_transform 是 viewport 级，change_scene 不自动重置；②Control Label 挂 Node2D 下 + 父 visible=false 的隐藏在本构建不可靠，直接删节点最稳；③GalgameDialog 自带 ESC 跳过（_skip_all）+ finished 信号，独立场景可直接复用。

### 紧急修复3：物理 flush 报错 + 横杠 + 黑屏不跳花海（2026-08-16 23:10）
- **报错1（RoomManager._build_blocked flushing queries）**：NextDoor/门 的 `body_entered` 回调里**同步** `_game.call("transition_to"/"_go_next_layer")` → _swap→setup→_build_blocked add_child StaticBody2D，此时物理引擎正在 flush queries（处理 body_entered）→ 报错。修：两处门回调改 `call_deferred`。
- **报错2（Enemy._drop / Boss 掉梦晶 flushing queries）**：敌人/ Boss 被弹道命中（Projectile._on_body_entered 物理回调）→ take_damage→_die→_drop `add_child(Area2D Pickup)` → 报错。修：`sc.call_deferred("add_child", pk)`。报错2 会中断 Boss._die → on_boss_defeated → 不跳花海（黑屏）。
- **横杠（第三关）**：project.godot `hide_blocked_visuals=false`+`hide_door_visuals=false` → 运行期画**禁区红色框 + 门绿色判定框**在游戏画面里 = 横杠/色块。修：新增 `RoomManager._debug_visuals_on()`（`Engine.is_editor_hint() or GameManager.dev_mode`），门判定框 `_add_door_visual` 与禁区红色可视化都只在 dev 模式/编辑器预览显示，正常游戏不画。
- **黑屏不跳花海**：报错2 中断 boss 死亡链所致；deferred 修复后 on_boss_defeated→_goto_epilogue→Epilogue 正常。
- **坑（关键）**：**物理回调（body_entered/area_entered）里绝不允许 add_child 碰撞体 / 改 shape 状态 / 切场景重建**，一律 `call_deferred`。Godot 报 "Can't change this state while flushing queries. Use call_deferred()"。

### 修复4：黑屏不跳花海（canvas_transform 残留）+ 第三关横杠（Layer Frame）（2026-08-16 23:20）
- **黑屏不跳花海真因**：不是 deferred 报错（上轮已修），而是 **Epilogue 场景没重置 viewport.canvas_transform**——Game 每帧写 canvas_transform 缩放跟随玩家，change_scene 到 Epilogue 后残留 → 花海背景 TextureRect 被错误缩放推到屏幕外 → 黑屏。修：`Epilogue._ready` 加 `get_viewport().set_canvas_transform(Transform2D.IDENTITY)`（与 Main._ready 同款）。
- **横杠真因**：不是判定框/禁区可视化（上轮 dev_mode 控制是对的），而是 **Layer1/2/3.tscn 的 `Ghost/Frame` Line2D 矩形框**（880×500 房间边界框 4 条边，Layer.gd 的 `visible=false` 对子节点不生效）→ 画面横线。修：python 删 3 个 tscn 全部 Frame 节点（22 个，与之前删 Title 同法）。
- **验证**：Epilogue 场景 -s 实例化 OK（FlowerField texture=true）；编译零错误；Layer 无 Line2D/Title 残留。
- **坑**：①**任何从 Game（手写 canvas_transform）change_scene 出去的目标场景都要重置 canvas_transform**（Main/Epilogue 已加；DeathCG/Birthday/WeaponSelect/Intro/Prologue 尚未加，若后续报错需补）；②Layer.gd 的 group 隐藏对子 CanvasItem（Label/Line2D）在本构建不可靠，直接删节点最稳。

### BGM 接入 + 打包准备（2026-08-16 23:55）
- **BGM 接入**：`Audio/Midnight_Protocol.mp3`（3.5MB）是整个游戏唯一 BGM → 复制到 `assets/audio/Midnight_Protocol.mp3`；`GameManager._build_bgm()` 建 AudioStreamPlayer（autoload 常驻，stream.loop=true 整首循环，volume_db=-8），`play_bgm()/stop_bgm()`；Game._ready 调 play_bgm（进玩法播，切房不中断）、Main._ready 调 stop_bgm（回标题停）。音量受之前 AudioServer master 总线设置控制。原 LevelData.BGM（bgm_1/2/3.ogg）文件不存在，暂未用。
- **打包**：用户选 Windows 桌面 + 单文件 exe + 命令行导出。已写 export_presets.cfg（embed_pck=true 单文件、product_name="梦境逐影 Dream Chaser"、export_path="build/梦境逐影.exe"）。导出模板 4.7.1 未安装，正在下载 tpz。

### 打包完成（2026-08-17 00:10）
- **导出成功**：`build/梦境逐影.exe`（662MB 单文件 exe，embed_pck=true）。
- **流程**：下载官方 tpz（1.19GiB，GitHub 断连 1 次用 `curl -C - --retry-all-errors` 续传）→ python zipfile 解压 12 个 Windows 模板 exe + version.txt 到 `%APPDATA%/Godot/export_templates/4.7.1.stable/` → 写 export_presets.cfg → `godot --headless --export-release "Windows Desktop" build/梦境逐影.exe`。
- **警告/待优化**：①UID duplicate（assets/Live2d 与 Artssucai/Live2d 的 momo 3 图 UID 撞）——Artssucai 源目录被 Godot 扫描所致，建议给 Artssucai 加 .gdignore；②exe 662MB 偏大——打包进了 _probe2/、_trash_v2/、Artssucai/ 等无关目录，建议 export_presets 加 exclude_filter 排除。

### 传送门 + 章节标题 + 台词重应用（2026-08-17 00:45）
- **f1_r1 跳花海传送门**：新增 `PortalHandle.gd`（@tool 继承 BlockedHandle，紫色判定框 + T-003 门图标，class_name PortalHandle）；RoomManager `_portal_handles` 收集（在 is BlockedHandle 前判断）+ `_build_portal`（Area2D + 门图标 + 「momo 专用·按 F 一键跳到结尾剧情」提示标签，body_entered→set_portal_nearby）；Game `_portal_nearby` + interact 分支 `_jump_to_epilogue()`（置 game_completed=true → change_scene Epilogue，花海播完回 Game 通关态）；f1_r1.tscn 加 Portal 节点（-230,-60，80×80 矩形）。
- **章节标题重设计**（用户确认）：第一章「无尽路途」、第二章「永不到站」、第三章「OKR」（替换原 第X章，`_show_chapter_card` names 数组）。
- **台词重应用**（以文档 5.3/5.4 为权威，总表「六」未同步旧版）：f2 Boss 前「好恶心的怪物啊，我可不想被它碰到」；f3 本体梦呓「……momo……momo……我喜欢你……」（文档原文 mopmo/moomo 系 typo，已修正为 momo）；花海相认加「我」（「那个、那个我一直看你直播的」）；新增谢幕字卡「献给弥绘 · 以及所有在深夜里努力生活的人。愿你们的梦里，都有好梦。」。
- **注意**：文档 5.3 与「六·对话总表」有多处矛盾（f2/f3 台词），以 5.3 章节为准。

### 章节旁白 + 类型推断报错修复（2026-08-17 01:00）
- **报错修复**：`var title := names[...]` 因 names 是未标注 Array（元素 Variant）无法推断类型 → 改 `var names: Array[String]` + `var title: String`（严格模式）。
- **章节旁白**：用户要第二三章标题下方加叙事文字（对标 Prologue 第一章「标题+旁白」结构）。`_show_chapter_card` 改为居中 VBox（大标题 72px 在上 + 旁白 26px 在下，separation 20）；第二章旁白「挤不上的地铁，停不下的脚步。别在这趟车上睡过去。」、第三章旁白「老板的无影手悬在头顶，OKR 的数字压得人喘不过气。推开那扇门，让噩梦下班。」；第一章 Prologue 标题「第一章 · 醒来」→「无尽路途 · 醒来」（统一章节名）。

### 三章样式统一 + 第一章旁白 + T-003 报错守卫（2026-08-17 01:30）
- **三章标题格式统一**（第X章 · 章节名）：第一章 Prologue「无尽路途」→「第一章 · 无尽路途」（加"第一章 ·"前缀）；第二三章已是「第二章 · 永不到站」「第三章 · OKR」。第二三章标题样式改 56px 金色 Color(0.95,0.85,0.65) 无描边（对齐第一章）。
- **第一章旁白**（用户提供）："凌晨的公路上,早班车的车灯划破黎明。/ 上班的路很远,梦却很近。/ 现在,穿过这片梦境走向梦醒的十分。"（用户原文"十分"，疑似"时分"笔误，已按原文保留）。
- **T-003 报错消除**：`T-003_door_frame_open_anim.png` png 本体已丢失（只剩 .import 残留，assets 全无门素材 png）。4 处 load（NextDoorHandle/PortalHandle/RoomManager×2）改 `ResourceLoader.exists()` 守卫——素材缺失时跳过不报错，用户素材准备好放到 `assets/tiles/T-003_door_frame_open_anim.png` 即自动生效。传送门功能保留（判定框+紫色框+提示标签），仅门图标贴图暂缺。

### 第二三章做成独立 tscn（同 Prologue 结构）（2026-08-17 01:35）
- **需求**：第二三章章节标题做成和 Prologue.tscn 一样的独立场景（不再代码动态生成黑屏卡片）。
- **实现**：新建 `ChapterCard.gd`（通用脚本，复制 Prologue.gd：任意键/点击推进、ESC 跳过、NEXT_SCENE=Game.tscn）+ `Chapter2.tscn`（第二章·永不到站 + 旁白「挤不上的地铁…」）+ `Chapter3.tscn`（第三章·OKR + 旁白「老板的无影手…」），结构=Background 深色 + ChapterTitle 56px 金色 + StoryText 26px 白多行 + Hint「按任意键继续」。
- **流程**：`Game._go_next_layer(next)` 改为 `change_scene_to_file("Chapter%d.tscn" % next)`（不再 transition_to + toast + _show_chapter_card），章节场景播完任意键 → change_scene(Game) → Game._ready `load_layer(layer_index)` + `transition_to(start_room(1))` 进新层起始房（start_room(1) 是硬编码"r1"/"r0"，在 load_layer 后的当前层解释）。删除了 `_show_chapter_card` 函数。
- **注意**：Game._ready 里 `transition_to(LevelData.start_room(1))` 是硬编码 1（返回"r1"，第3层返回"r0"），在 load_layer(layer_index) 之后执行，实际进的是当前层起始房——切层回 Game 依赖这个机制。

### 角色 UI 面板 CharacterUI.tscn（2026-08-17 01:40）
- **需求**：按参考图（圆头像+HP红+MP蓝+EXP金+GV金币）生成新角色 UI，头像自动放立绘+圆形蒙板裁剪，tscn 化。
- **实现**：
  - 圆形蒙板：`assets/ui/mask_circle_256.png`（PIL 生成 256×256 白圆透明背景）。
  - `CharacterUI.tscn`：Control 全屏；AvatarFrame(256×256 左上)含 PortraitRect(Live2d/momo_happy.png, STRETCH_KEEP_ASPECT_COVERED) + Mask(mask_circle_256.png)裁剪为圆；右侧 HBox 三条 HP/MP/EXP（图标 Label"♥♦★"彩字 + "HP/MP/EXP"白字描边 + 条形——HP/EXP 用 UI-001~004 frame+fill 素材，MP 用 Panel+StyleBoxFlat 蓝条+白边）；右下角梦晶图标(UI-010)+"GV 1"金字。
  - `CharacterUI.gd`：基础脚本，_refresh 占位待接 GameManager.stats_changed。
- **坑**：①MP 没现成 frame+fill 素材，用 Panel+StyleBoxFlat 自画；②小图标（♥♦★）用 Label 字符+theme_override_color 着色，避开缺失的心形/水滴/星星 png；③Godot 4 Mask 节点为 Control 子节点，用黑白贴图定义父可见区（白色=可见）。

### CharacterUI 完整 tscn 替换旧 HUD（2026-08-17 02:05）
- **需求澄清**：参考图 11.png 做**一整个 tscn**（圆头像+HP/MP/EXP 三条+GV），旧 HUD 删掉改用 tscn。之前"只有头像"版本理解错了。
- **实现**：`CharacterUI.tscn` 重写为完整版（28 节点）：左上 AvatarBox 256（PortraitRect 立绘 KEEP_ASPECT_COVERED + RingMask 圆外遮挡 + GoldRing StyleBoxFlat 金色圆框 corner 128）；右侧三条 ProgressBar（HP 用 UI-001/002 StyleBoxTexture 9-slice、MP 用 StyleBoxFlat 蓝、EXP 用 UI-003/004）+ ♥♦★ 图标+名字；InfoBox（Lv/暴击/武器/技能，继承旧 HUD 功能）；右下 GVRow（UI-010+GV 金字）；底部操作提示。`CharacterUI.gd`：_process 每帧读 GameManager 刷新 7 处。
- **替换**：Game.tscn ext_resource 2 由 HUD.gd 脚本 → CharacterUI.tscn PackedScene；HUD CanvasLayer 保留（**必须**：Control 挂 Game 根 Node2D 下会受手写 canvas_transform 缩放跟随乱飞），CharacterUI 作为 HUD 子实例。删 `src/ui/HUD.gd`+uid。
- **坑**：①改 tscn 用 python 先 `c.replace('\r\n','\n')` 归一化（Godot 存 CRLF，断言 LF 字符串必失败）；②无头 -s 测试报 "Identifier not found: GameManager" 是 autoload 不加载的正常现象，--editor 编译零错误即可；③血条改 ProgressBar（theme_override_styles/background+fill 吃 9-slice），别再手写 ColorRect 缩放。

### UI 四项微调（2026-08-17 02:20）
- **头像下移**：CharacterUI.tscn PortraitRect 加 `offset_top/offset_bottom = 40`（整体下移 40px，脸进圆框）。用户自己调法：编辑器选中 AvatarBox/PortraitRect，改 offset_top 和 offset_bottom **同增同减**（比如都想再下移就都 60）。
- **拾取/toast 右上角**：Game `_build_pickup_prompt` position → `(vsize.x-w-24, 62)`；`_build_toast` → `(vsize.x-580, 20)` + size(560,28) + `HORIZONTAL_ALIGNMENT_RIGHT` 右对齐。
- **操作提示可隐藏**：CharacterUI.tscn Hint 右侧加 ToggleHint「按 H 隐藏」；CharacterUI.gd `_unhandled_input` KEY_H 切 `_hint.visible` + 文案切换「按 H 显示/隐藏」。
- **传送门 scale 生效**：用户是在 f1_r1.tscn 调 Portal 节点的 **scale**（0.405）而非 rect_size——原 `_build_portal` 忽略 h.scale。修：判定框 `rsh.size = h.rect_size * h.scale` + 图标 `spr.scale = rect_size*scale / tex尺寸`。（str.replace 全替换同款代码，_build_inn 驿站判定框也×scale——手柄 scale 生效，行为一致无害。）
- **坑**：tscn 用户编辑器改动后 unique_id 会变（Godot 重写），python 断言要用不依赖 unique_id 的锚点。
